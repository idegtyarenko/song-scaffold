/**
 * The React root. It is deliberately empty: the setup form, the transport and the beat
 * indicator still live in the imperative `main.ts` and move here one screen at a time.
 */
export function App() {
  return null;
}
